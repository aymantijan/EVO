{
  "buildCommand": "pip install -r requirements.txt && python manage.py collectstatic --noinput",
  "startCommand": "python manage.py migrate --noinput && gunicorn gamification_config.wsgi --log-file -"
}
